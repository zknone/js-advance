{
  "name": "js-advance",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "start": "npm run build && npm run preview",
    "preview": "vite preview",
    "format": "prettier --write ."
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "sass": "^1.89.2",
    "typescript": "~5.8.3",
    "vite": "^5.2.10"
  },
  "dependencies": {
    "handlebars": "^4.7.8"
  }
}
